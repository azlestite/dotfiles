#!/usr/bin/env bash
#
# shellcheck disable=all

export CHEZMOI_REPO_ROOT="{{ .chezmoi.workingTree }}"

{{ template "utils.sh" . }}

{{ if (and (eq .chezmoi.os "linux") (eq .chezmoi.osRelease.idLike "debian")) }}
  {{ if .private_setup}}
    # {{- include "../install/ubuntu/chezmoi_private.sh" }}
    info "Start private setup..."
  {{ end }}
{{ end }}
